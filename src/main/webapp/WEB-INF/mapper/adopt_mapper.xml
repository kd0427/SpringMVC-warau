<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
						"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
						
<mapper namespace="adopt">
	<!-- 게시판 리스트 -->
	<select id="getList" resultType="com.global.vo.AdoptVO">
		<![CDATA[
			select * from adopt_table order by adopt_idx desc
		]]>
	</select>
	
	<!-- 글쓰기 -->
	<insert id="write" parameterType="com.global.vo.AdoptVO">
	<![CDATA[
		insert into adopt_table(adopt_idx, adopt_title, adopt_content, adopt_regdate, adopt_img, adopt_writer)
			values (adopt_seq.nextval, #{adopt_title}, #{adopt_content}, sysdate, #{adopt_img, jdbcType=VARCHAR},
			#{adopt_writer})
	]]>
	</insert>
</mapper>
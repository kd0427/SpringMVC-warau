<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
						"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user">
	<select id="checkUserIdExist" parameterType='java.lang.String' resultType="java.lang.String">
		<![CDATA[
			select user_name
			from user_table
			where user_id= #{user_id}
		]]>
	</select>
	
	<insert id="addUserInfo" parameterType='com.global.vo.UserVO'>
		<![CDATA[
			insert into user_table(user_id, user_pw, user_name, user_email, user_pet, user_date)
			values(#{user_id}, #{user_pw}, #{user_name}, #{user_email}, #{user_pet}, sysdate)
		]]>
	</insert>
	
	<select id="getLoginUserInfo" parameterType='com.global.vo.UserVO' resultType='com.global.vo.UserVO'>
		<![CDATA[
			select user_id, user_name
			from user_table
			where user_id=#{user_id} and user_pw=#{user_pw}
		]]>
	</select>
	
	<select id="getModifyUserInfo" parameterType='java.lang.String' resultType='com.global.vo.UserVO'>
			select *
			from user_table
			where user_id=#{user_id}
	</select>
	
	<update id="modifyUserInfo" parameterType='com.global.vo.UserVO'>
		update user_table
		set user_pw = #{user_pw}
		where user_id = #{user_id}
	</update>
	
</mapper>